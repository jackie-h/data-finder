from __future__ import annotations
from typing import Union
from datafinder.typed_attributes import *
from datafinder import FinderResult, convert_inputs_and_select, convert_inputs_and_select_for_date_range
from datafinder.attribute import Attribute
from datafinder.finder import RelatedFinder
from model.relational import Column, Table, NoOperation, JoinOperation, JoinTreeNodeOperation, CountAllOperation, Operation
from model.milestoning import ProcessingTemporalColumns, SingleBusinessDateColumn, BusinessDateAndProcessingTemporalColumns, BiTemporalColumns, MilestonedTable
from {{class_module_map_base[rcm.clazz.name]}} import {{rcm.clazz.name}}FinderBase, {{rcm.clazz.name}}RelatedFinderBase

{% for rpm in rcm.property_mappings %}
{% if not is_primitive(rpm.property) and rpm.property.type.name != rcm.clazz.name %}from {{class_module_map[rpm.property.type.name]}} import {{rpm.property.type.name}}RelatedFinder
{% endif %}
{% endfor %}

{% for rpm in rcm.property_mappings %}
{% if not is_primitive(rpm.property) and rpm.property.type.name != rcm.clazz.name and flatten_embedded(rpm.target) %}
def _embedded_for_{{to_python_name(rpm.property)}}(incoming: dict | None, parent, relation_name: str) -> dict:
    """Join-less alternates for '{{rpm.property.id}}.*' properties satisfied by flat columns
    on this table, merged with any deeper embedding forwarded by the caller."""
    result: dict = {}
{% for dotted, leaf_prop, col in flatten_embedded(rpm.target) %}
    result['{{dotted}}'] = {{leaf_prop.type.name}}Attribute(relation_name.title() + ' {{display_name(leaf_prop)}}', '{{col.name}}', '{{col.type}}', '{{table_qualified_name(col.table)}}', parent)
{% endfor %}
    if incoming:
        result.update({k[len('{{rpm.property.id}}.'):]: v for k, v in incoming.items() if k.startswith('{{rpm.property.id}}.')})
    return result

{% endif %}
{% endfor %}

class {{rcm.clazz.name}}RelatedFinder({{rcm.clazz.name}}RelatedFinderBase, RelatedFinder):

{% if rcm.milestone_mapping %}
{% if has_bitemporal(rcm.milestone_mapping) %}
    __zz_milestoning_columns = BiTemporalColumns(Column('{{rcm.milestone_mapping._date_from.target.name}}', '{{rcm.milestone_mapping._date_from.target.type}}'),
                                                 Column('{{rcm.milestone_mapping._date_to.target.name}}', '{{rcm.milestone_mapping._date_to.target.type}}'),
                                                 Column('{{rcm.milestone_mapping._in.target.name}}', '{{rcm.milestone_mapping._in.target.type}}'),
                                                 Column('{{rcm.milestone_mapping._out.target.name}}', '{{rcm.milestone_mapping._out.target.type}}'){% if rcm.milestone_mapping.infinite_datetime %}, infinite_datetime='{{rcm.milestone_mapping.infinite_datetime}}'{% endif %})
{% elif has_business_date_and_processing(rcm.milestone_mapping) %}
    __zz_milestoning_columns = BusinessDateAndProcessingTemporalColumns(Column('{{rcm.milestone_mapping._date.target.name}}', '{{rcm.milestone_mapping._date.target.type}}'),
                                                                        Column('{{rcm.milestone_mapping._in.target.name}}', '{{rcm.milestone_mapping._in.target.type}}'),
                                                                        Column('{{rcm.milestone_mapping._out.target.name}}', '{{rcm.milestone_mapping._out.target.type}}'){% if rcm.milestone_mapping.infinite_datetime %}, infinite_datetime='{{rcm.milestone_mapping.infinite_datetime}}'{% endif %})
{% elif has_processing_temporal(rcm.milestone_mapping) %}
    __zz_milestoning_columns = ProcessingTemporalColumns(Column('{{rcm.milestone_mapping._in.target.name}}', '{{rcm.milestone_mapping._in.target.type}}'),
                                                         Column('{{rcm.milestone_mapping._out.target.name}}', '{{rcm.milestone_mapping._out.target.type}}'){% if rcm.milestone_mapping.infinite_datetime %}, infinite_datetime='{{rcm.milestone_mapping.infinite_datetime}}'{% endif %})
{% elif has_uni_business_temporal(rcm.milestone_mapping) %}
    __zz_milestoning_columns = SingleBusinessDateColumn(Column('{{rcm.milestone_mapping._date.target.name}}', '{{rcm.milestone_mapping._date.target.type}}'))
{% endif %}
    __table = MilestonedTable('{{table_qualified_name(rcm.property_mappings[0].target.table)}}', [], __zz_milestoning_columns)
{% else %}
    __table = Table('{{table_qualified_name(rcm.property_mappings[0].target.table)}}', [])
{% endif %}

    _column_map = {
{% for rpm in rcm.property_mappings %}
{% if is_primitive(rpm.property) %}
        '{{rpm.property.id}}': ('{{rpm.target.name}}', '{{rpm.target.type}}', '{{table_qualified_name(rpm.target.table)}}'),
{% endif %}
{% endfor %}
    }

    def __init__(self, relation_name: str, source: Column, target: Column, parent_join: JoinTreeNodeOperation | None = None,
                 embedded: dict | None = None, **kwargs):
        join = JoinOperation(relation_name, {{rcm.clazz.name}}RelatedFinder.__table, source, target)
        self.__node = JoinTreeNodeOperation(join, parent_join)
        RelatedFinder.__init__(self, self.__node)
{% for rpm in rcm.property_mappings %}
{% if is_primitive(rpm.property) %}
        self.__{{to_python_name(rpm.property)}} = {{rpm.property.type.name}}Attribute(relation_name.title() + ' {{display_name(rpm.property)}}', '{{rpm.target.name}}', '{{rpm.target.type}}', '{{table_qualified_name(rpm.target.table)}}', self.__node, embedded=(embedded or {}).get('{{rpm.property.id}}'))
{% endif %}
{% endfor %}
{% for rpm in rcm.property_mappings %}
{% if not is_primitive(rpm.property) and rpm.property.type.name != rcm.clazz.name %}
        self.__{{to_python_name(rpm.property)}} = {{rpm.property.type.name}}RelatedFinder('{{rpm.property.name}}', Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'),Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'), self.__node, embedded={% if flatten_embedded(rpm.target) %}_embedded_for_{{to_python_name(rpm.property)}}(embedded, self.__node, '{{rpm.property.name}}'){% else %}{k[len('{{rpm.property.id}}.'):]: v for k, v in (embedded or {}).items() if k.startswith('{{rpm.property.id}}.')} or None{% endif %})
{% endif %}
{% endfor %}
{% for source_rcm, rpm, assoc, reverse_name in reverse_assocs %}
        self.__{{reverse_name}} = None
{% endfor %}
        if kwargs:
            join.filter_kwargs = kwargs
            join.filter_column_map = {{rcm.clazz.name}}RelatedFinder._column_map

{% for rpm in rcm.property_mappings %}
{% if is_primitive(rpm.property) %}
    def {{to_python_name(rpm.property)}}(self) -> {{rpm.property.type.name}}Attribute:
{% if rpm.property.tagged_values['doc'] %}
        """{{display_name(rpm.property)}}
        {{rpm.property.tagged_values['doc'].value}}
        """
{% else %}
        """{{display_name(rpm.property)}}"""
{% endif %}
        return self.__{{to_python_name(rpm.property)}}

{% elif rpm.property.type.name != rcm.clazz.name %}
    def {{to_python_name(rpm.property)}}(self, **kwargs) -> {{rpm.property.type.name}}RelatedFinder:
{% if rpm.property.tagged_values['doc'] %}
        """{{display_name(rpm.property)}}
        {{rpm.property.tagged_values['doc'].value}}
        """
{% else %}
        """{{display_name(rpm.property)}}"""
{% endif %}
        if kwargs:
            return {{rpm.property.type.name}}RelatedFinder('{{rpm.property.name}}', Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'), Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'), self.__node, **kwargs)
        return self.__{{to_python_name(rpm.property)}}

{% endif %}
{% endfor %}
{% for source_rcm, rpm, assoc, reverse_name in reverse_assocs %}
    def {{reverse_name}}(self, **kwargs) -> {{source_rcm.clazz.name}}RelatedFinder:
        """{{source_rcm.clazz.name}}"""
        if kwargs:
            from {{class_module_map[source_rcm.clazz.name]}} import {{source_rcm.clazz.name}}RelatedFinder
            return {{source_rcm.clazz.name}}RelatedFinder('{{source_rcm.clazz.name}}', Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'), Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'), self.__node, **kwargs)
        if self.__{{reverse_name}} is None:
            from {{class_module_map[source_rcm.clazz.name]}} import {{source_rcm.clazz.name}}RelatedFinder
            self.__{{reverse_name}} = {{source_rcm.clazz.name}}RelatedFinder('{{source_rcm.clazz.name}}', Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'),Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'), self.__node)
        return self.__{{reverse_name}}

{% endfor %}

class {{rcm.clazz.name}}Finder({{rcm.clazz.name}}FinderBase):
{% if rcm.clazz.tagged_values['doc'] %}
    """{{rcm.clazz.tagged_values['doc'].value}}
    """
{% endif %}

{% if rcm.milestone_mapping %}
{% if has_bitemporal(rcm.milestone_mapping) %}
    __zz_milestoning_columns = BiTemporalColumns(Column('{{rcm.milestone_mapping._date_from.target.name}}', '{{rcm.milestone_mapping._date_from.target.type}}'),
                                                 Column('{{rcm.milestone_mapping._date_to.target.name}}', '{{rcm.milestone_mapping._date_to.target.type}}'),
                                                 Column('{{rcm.milestone_mapping._in.target.name}}', '{{rcm.milestone_mapping._in.target.type}}'),
                                                 Column('{{rcm.milestone_mapping._out.target.name}}', '{{rcm.milestone_mapping._out.target.type}}'){% if rcm.milestone_mapping.infinite_datetime %}, infinite_datetime='{{rcm.milestone_mapping.infinite_datetime}}'{% endif %})
{% elif has_business_date_and_processing(rcm.milestone_mapping) %}
    __zz_milestoning_columns = BusinessDateAndProcessingTemporalColumns(Column('{{rcm.milestone_mapping._date.target.name}}', '{{rcm.milestone_mapping._date.target.type}}'),
                                                                        Column('{{rcm.milestone_mapping._in.target.name}}', '{{rcm.milestone_mapping._in.target.type}}'),
                                                                        Column('{{rcm.milestone_mapping._out.target.name}}', '{{rcm.milestone_mapping._out.target.type}}'){% if rcm.milestone_mapping.infinite_datetime %}, infinite_datetime='{{rcm.milestone_mapping.infinite_datetime}}'{% endif %})
{% elif has_processing_temporal(rcm.milestone_mapping) %}
    __zz_milestoning_columns = ProcessingTemporalColumns(Column('{{rcm.milestone_mapping._in.target.name}}', '{{rcm.milestone_mapping._in.target.type}}'),
                                                         Column('{{rcm.milestone_mapping._out.target.name}}', '{{rcm.milestone_mapping._out.target.type}}'){% if rcm.milestone_mapping.infinite_datetime %}, infinite_datetime='{{rcm.milestone_mapping.infinite_datetime}}'{% endif %})
{% elif has_uni_business_temporal(rcm.milestone_mapping) %}
    __zz_milestoning_columns = SingleBusinessDateColumn(Column('{{rcm.milestone_mapping._date.target.name}}', '{{rcm.milestone_mapping._date.target.type}}'))
{% endif %}
    __table = MilestonedTable('{{table_qualified_name(rcm.property_mappings[0].target.table)}}', [], __zz_milestoning_columns)
{% else %}
    __table = Table('{{table_qualified_name(rcm.property_mappings[0].target.table)}}', [])
{% endif %}

{% for rpm in rcm.property_mappings %}
{% if is_primitive(rpm.property) %}
    __{{to_python_name(rpm.property)}} = {{rpm.property.type.name}}Attribute('{{display_name(rpm.property)}}', '{{rpm.target.name}}', '{{rpm.target.type}}', '{{table_qualified_name(rpm.target.table)}}')
{% endif %}
{% endfor %}
{% for rpm in rcm.property_mappings %}
{% if not is_primitive(rpm.property) %}
    __{{to_python_name(rpm.property)}} = {{rpm.property.type.name}}RelatedFinder('{{rpm.property.name}}', Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'),Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'){% if rpm.property.type.name != rcm.clazz.name and flatten_embedded(rpm.target) %}, embedded=_embedded_for_{{to_python_name(rpm.property)}}(None, None, '{{rpm.property.name}}'){% endif %})
{% endif %}
{% endfor %}
{% if reverse_assocs %}

    def __init__(self):
{% for source_rcm, rpm, assoc, reverse_name in reverse_assocs %}
        self.__{{reverse_name}} = None
{% endfor %}

{% endif %}
{% for rpm in rcm.property_mappings %}
{% if is_primitive(rpm.property) %}
    def {{to_python_name(rpm.property)}}(self) -> {{rpm.property.type.name}}Attribute:
{% if rpm.property.tagged_values['doc'] %}
        """{{display_name(rpm.property)}}
        {{rpm.property.tagged_values['doc'].value}}
        """
{% else %}
        """{{display_name(rpm.property)}}"""
{% endif %}
        return {{rcm.clazz.name}}Finder.__{{to_python_name(rpm.property)}}

{% else %}
    def {{to_python_name(rpm.property)}}(self, **kwargs) -> {{rpm.property.type.name}}RelatedFinder:
{% if rpm.property.tagged_values['doc'] %}
        """{{display_name(rpm.property)}}
        {{rpm.property.tagged_values['doc'].value}}
        """
{% else %}
        """{{display_name(rpm.property)}}"""
{% endif %}
        if kwargs:
            return {{rpm.property.type.name}}RelatedFinder('{{rpm.property.name}}', Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'), Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'), None, **kwargs)
        return {{rcm.clazz.name}}Finder.__{{to_python_name(rpm.property)}}

{% endif %}
{% endfor %}
{% for source_rcm, rpm, assoc, reverse_name in reverse_assocs %}
    def {{reverse_name}}(self, **kwargs) -> {{source_rcm.clazz.name}}RelatedFinder:
        """{{source_rcm.clazz.name}}"""
        if kwargs:
            from {{class_module_map[source_rcm.clazz.name]}} import {{source_rcm.clazz.name}}RelatedFinder
            return {{source_rcm.clazz.name}}RelatedFinder('{{source_rcm.clazz.name}}', Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'), Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'), None, **kwargs)
        if self.__{{reverse_name}} is None:
            from {{class_module_map[source_rcm.clazz.name]}} import {{source_rcm.clazz.name}}RelatedFinder
            self.__{{reverse_name}} = {{source_rcm.clazz.name}}RelatedFinder('{{source_rcm.clazz.name}}', Column('{{rpm.target.rhs.name}}', '{{rpm.target.rhs.type}}', '{{table_qualified_name(rpm.target.rhs.table)}}'),Column('{{rpm.target.lhs.name}}', '{{rpm.target.lhs.type}}', '{{table_qualified_name(rpm.target.lhs.table)}}'))
        return self.__{{reverse_name}}

{% endfor %}
    def count(self) -> CountAllOperation:
        """Count all rows in the underlying table."""
        return CountAllOperation('{{table_qualified_name(rcm.property_mappings[0].target.table)}}')

    def find_all(self,
                 business_date: Union[datetime.date, str],
                 processing_valid_at: Union[datetime.datetime, str],
                 display_columns: list[Attribute],
                 filter_op: Operation = NoOperation()) -> FinderResult:
        """Returns data matching the optional filter."""
{% if rcm.milestone_mapping %}
{% if has_bitemporal(rcm.milestone_mapping) %}
        return convert_inputs_and_select(business_date, processing_valid_at, display_columns, {{rcm.clazz.name}}Finder.__table, filter_op)  # type: ignore[arg-type]
{% elif has_business_date_and_processing(rcm.milestone_mapping) %}
        return convert_inputs_and_select(business_date, processing_valid_at, display_columns, {{rcm.clazz.name}}Finder.__table, filter_op)  # type: ignore[arg-type]
{% elif has_processing_temporal(rcm.milestone_mapping) %}
        return convert_inputs_and_select(None, processing_valid_at, display_columns, {{rcm.clazz.name}}Finder.__table, filter_op)  # type: ignore[arg-type]
{% elif has_uni_business_temporal(rcm.milestone_mapping) %}
        return convert_inputs_and_select(business_date, processing_valid_at, display_columns, {{rcm.clazz.name}}Finder.__table, filter_op)  # type: ignore[arg-type]
{% endif %}
{% else %}
        return convert_inputs_and_select(None, None, display_columns, {{rcm.clazz.name}}Finder.__table, filter_op)  # type: ignore[arg-type]
{% endif %}
{% if rcm.milestone_mapping and (has_bitemporal(rcm.milestone_mapping) or has_business_date_and_processing(rcm.milestone_mapping) or has_uni_business_temporal(rcm.milestone_mapping)) %}

    def find_for_date_range(self,
                            business_date_from: Union[datetime.date, str],
                            business_date_to: Union[datetime.date, str],
                            processing_valid_at: Union[datetime.datetime, str],
                            display_columns: list[Attribute],
                            filter_op: Operation = NoOperation()) -> FinderResult:
        """Returns data for all records whose business date overlaps the given range.

        For bitemporal tables the range uses interval overlap:
            business_date_from_col <= business_date_to AND business_date_to_col > business_date_from
        For single-date tables:
            business_date >= business_date_from AND business_date <= business_date_to
        """
        return convert_inputs_and_select_for_date_range(business_date_from, business_date_to, processing_valid_at, display_columns, {{rcm.clazz.name}}Finder.__table, filter_op)  # type: ignore[arg-type]
{% endif %}
